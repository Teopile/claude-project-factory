# Master Spec — <PROJECT>

> Source of truth. Extreme detail. Written for agents. Keep in sync with reality.

## 1. Overview
- Problem / goal:
- Target users:
- Success criteria:
- Chosen stack (+ why → decision-log):

## 2. Information architecture
- Sitemap / routes:
- Navigation model:

## 3. Pages / screens
For each page: purpose, layout, every component, every button + exact behavior,
every state (default / loading / empty / error / success), data shown, permissions.

### Page: <name> (`/route`)
- Purpose:
- Layout & components:
- Buttons & actions: `<button>` → `<exactly what happens>`
- States:
- Data / API used:

## 4. Data model
- Entities, fields, types, relations, constraints.

## 5. API / contracts
- `METHOD /path` → request, response, errors, authz.

## 6. Cross-cutting
- Auth, roles/permissions, validation rules, error handling, i18n, a11y,
  performance budgets, security requirements, analytics.

## 7. Non-goals / out of scope

## 8. Global acceptance criteria
