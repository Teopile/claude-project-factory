---
description: Ghostwriter - polished, ready-to-paste text in your voice, no meta-commentary.
argument-hint: <what to write + any context/voice notes>
---
Ghostwrite the following as finished, ready-to-paste text: $ARGUMENTS

- Output ONLY the deliverable - no preamble, no options, no "here's a draft."
- Match the user's voice and the channel's register; if samples or context are
  given, mirror them.
- Make it final, not a sketch. If one detail is genuinely blocking, ask;
  otherwise make a sensible choice and note it in a single line after the text.
