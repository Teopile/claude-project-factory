---
description: Debug an error or failing behavior.
argument-hint: <error, stack trace, or symptom>
---
Debug the following. Use the `debugging-wizard` skill if available; otherwise
apply its method: $ARGUMENTS

Parse the error, trace execution, form hypotheses, isolate the root cause from
the evidence, then propose the minimal fix. No blind guess-and-check.
